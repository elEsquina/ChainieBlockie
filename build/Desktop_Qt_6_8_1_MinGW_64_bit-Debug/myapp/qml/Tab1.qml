import QtQuick 2.15

Rectangle {
    color: "transparent" // Inherits the parent's dark background

    Text {
        anchors.centerIn: parent
        text: "Tab Content"
        font.pixelSize: 24
        color: "#3498db" // Blue text
    }
}
